import React from 'react'
import { View } from 'react-native'

import SocialIcon from '../components/SocialIcon'
import { Container } from '../../components'

const Login = () => {
  return (
    <Container footer={<SocialIcon />}>
      <View />
    </Container>
  )
}

export default Login